---
title: "Accordion"
description: "An interactive collection of stacked headings that reveal individual content sections with a click."
thumbnail: { src: "/images/components/buttons.png", alt: "" }
link: "/components/accordion"
summary: "This is my first post!"
---

<ComponentPreview name="accordion-demo" />

## How it works

<Steps>

<Step>Install the following dependencies:</Step>

```bash
npm install @radix-ui/react-accordion lucide-react tailwind-merge clsx
```

<Step>Create a new file at `lib/utils.ts` and add the provided code.</Step>

```tsx
import clsx, { ClassValue } from "clsx";
import { twMerge } from "tailwind-merge";

export function cn(...inputs: ClassValue[]) {
    return twMerge(clsx(inputs));
}
```

<Step>
    Create a file at `components/ui/accordion.tsx` and paste the given code.
</Step>

`accordion.tsx`

<CollapsibleSource name="accordion" />

<Step>Import and use the Accordion component in your application.</Step>

<CollapsibleSource  name="accordion-demo"/>
</Steps>
