---
title: "Bar Chart"
description: "A dynamic component for smooth transitions between panels."
thumbnail: { src: "/images/components/bar-chart.png", alt: "Bar Chart example" }
link: "/charts/bar-chart"
summary: "Enhanced user experience with animated transitions."
---

<ComponentPreview name="bar-chart-demo" />

## Installation

<Steps>
<Step>
Install the following dependencies:
</Step>

```bash 
npm install recharts motion
```

<Step>Create a file at `components/ui/card.tsx` and paste the given code.</Step>

<CollapsibleSource name="card" />

<Step>
    Create a file at `components/motion/chart.tsx` and paste the given code.
</Step>

<CollapsibleSource name="chart" />

<Step>Update the import paths to match your project setup.</Step>

</Steps>
